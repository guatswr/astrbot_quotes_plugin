{
  "storage": {
    "description": "数据存储根目录",
    "type": "string",
    "default": "",
    "hint": "留空则自动使用 AstrBot 的 data/plugin_data/quotes/ 目录",
    "obvious_hint": true
  },
  "git_backup": {
    "description": "Git 自动备份",
    "type": "object",
    "items": {
      "enabled": {
        "type": "bool",
        "default": false,
        "description": "启用自动备份",
        "hint": "要求数据目录本身是已配置上游分支的独立 Git 仓库"
      },
      "interval_minutes": {
        "type": "int",
        "default": 1440,
        "description": "备份间隔（分钟）",
        "hint": "启用后会在插件启动时立即检查一次，之后按此间隔执行"
      },
      "commit_message": {
        "type": "string",
        "default": "chore: automatic quotes backup ({timestamp})",
        "description": "自动提交信息",
        "hint": "可使用 {timestamp} 插入带时区的本地时间"
      },
      "command_timeout_seconds": {
        "type": "int",
        "default": 120,
        "description": "Git 命令超时（秒）",
        "hint": "单条 git 命令超过该时间会终止，下一轮自动重试"
      }
    }
  },
  "global_mode": {
    "description": "全局模式",
    "type": "bool",
    "default": false,
    "hint": "开启后随机语录在所有群/私聊间共享；关闭则仅限当前会话"
  },
  "delete_permission": {
    "description": "删除权限级别",
    "type": "string",
    "options": [
      "群员",
      "管理员",
      "群主",
      "Bot管理员"
    ],
    "default": "管理员",
    "hint": "群员：所有人可删 | 管理员：群管+群主 | 群主：仅群主 | Bot管理员：仅机器人管理员"
  },
  "blacklist": {
    "description": "语录黑名单（QQ号列表）",
    "type": "list",
    "default": [],
    "hint": "在此列表中的用户无法被上传语录，仅填写纯数字 QQ 号",
    "obvious_hint": true
  },
  "avatar_provider": {
    "description": "头像来源",
    "type": "string",
    "options": [
      "qlogo",
      "platform"
    ],
    "default": "qlogo",
    "hint": "qlogo：QQ 头像接口 | platform：平台接口（需 Napcat 权限）"
  },
  "image": {
    "description": "语录卡片样式",
    "type": "object",
    "items": {
      "width": {
        "type": "int",
        "default": 1280,
        "description": "图片宽度",
        "hint": "单位：像素(px)"
      },
      "height": {
        "type": "int",
        "default": 427,
        "description": "图片高度",
        "hint": "单位：像素(px)"
      },
      "bg_color": {
        "type": "string",
        "default": "#000",
        "description": "背景颜色",
        "hint": "右侧文字区域背景色，支持十六进制颜色值"
      },
      "text_color": {
        "type": "string",
        "default": "#fff",
        "description": "文字颜色",
        "hint": "语录文字颜色，支持十六进制颜色值"
      },
      "font_family": {
        "type": "string",
        "default": "-apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', 'WenQuanYi Micro Hei', Arial, sans-serif",
        "description": "字体",
        "hint": "CSS 字体栈格式，支持多个字体回退"
      }
    }
  },
  "image_signature_use_group": {
    "description": "使用群名片作为语录签名",
    "type": "bool",
    "default": false,
    "hint": "开启后语录图片右下角显示群名片，否则显示 QQ 昵称"
  },
  "performance": {
    "description": "性能设置",
    "type": "object",
    "items": {
      "text_mode": {
        "type": "bool",
        "default": false,
        "description": "纯文本模式",
        "hint": "开启后纯文字语录不渲染图片"
      },
      "render_cache": {
        "type": "bool",
        "default": true,
        "description": "渲染缓存",
        "hint": "上传纯文字语录后在后台预渲染并缓存图片"
      },
      "render_wait_timeout": {
        "type": "float",
        "default": 0.8,
        "description": "冷渲染等待时间",
        "hint": "随机语录未命中缓存时最多等待的秒数，超时先发送纯文本，后台继续渲染"
      }
    }
  },
  "poke_enabled": {
    "description": "启用戳一戳触发",
    "type": "bool",
    "default": false,
    "hint": "开启后对 Bot 戳一戳可随机发送语录"
  },
  "poke_probability": {
    "description": "戳一戳触发概率",
    "type": "int",
    "default": 20,
    "hint": "0-100 的整数，100 表示必触发"
  },
  "poke_group_whitelist": {
    "description": "群白名单",
    "type": "list",
    "default": [],
    "hint": "仅这些群的戳一戳会触发；留空则不限制（白名单优先）"
  },
  "poke_group_blacklist": {
    "description": "群黑名单",
    "type": "list",
    "default": [],
    "hint": "这些群的戳一戳不会触发；若同时配置白名单，则白名单优先"
  }
}
